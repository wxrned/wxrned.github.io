{
  "name": "your-project-name",
  "version": "1.0.0",
  "description": "Six, six, six. I'm so fucking sick.",
  "main": "updateMetaTags.js",
  "scripts": {
    "start": "node updateMetaTags.js"
  },
  "dependencies": {
    "node-fetch": "^3.3.0", // Ensure you use the latest version that supports ESM
    "color-thief": "^2.3.0", // Adjust the version as necessary
    "canvas": "^2.8.0" // Only if you're using canvas for image processing
  },
  "type": "module", // Use this if you want to use ES modules
  "engines": {
    "node": ">=16.0.0" // Ensure you have a compatible Node.js version
  }
}
